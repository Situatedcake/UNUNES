# Маркетплейс - Полнофункциональный интернет-магазин

## Установка и запуск

### Требования
- Node.js 16+ 
- npm или yarn

### Быстрый старт

1. Установите зависимости:
```bash
npm install
```

2. Запустите приложение (сервер и клиент одновременно):
```bash
npm run dev
```

Это запустит:
- **Сервер**: http://localhost:5000 (Express API)
- **Клиент**: http://localhost:5173 (Vite React)

3. Откройте браузер и перейдите на http://localhost:5173

## Функциональность

### 👤 Аутентификация
- Регистрация новых пользователей
- Вход в систему
- Сеанс хранится в cookies
- Данные пользователей сохраняются в JSON (`server/data/users.json`)

### 🛍️ Каталог товаров
- Просмотр всех товаров
- **Поиск по названию, описанию и категории** (в реальном времени)
- Фильтрация товаров
- Просмотр подробной информации о товаре в модальном окне
- Товары загружаются из JSON (`server/data/products.json`)

### 🛒 Корзина
- Добавление товаров в корзину
- Изменение количества товара
- Удаление товаров из корзины
- Подсчёт общей стоимости
- Данные корзины привязаны к пользователю (сохраняются в `server/data/cart.json`)

### 🔐 Админ-панель
- Доступна только для админов
- **Добавление новых товаров**
- **Редактирование существующих товаров**
- **Удаление товаров**
- Все изменения сохраняются в `server/data/products.json`

## Структура проекта

```
mpENIS/
├── server/
│   ├── data/
│   │   ├── users.json       # Данные пользователей
│   │   ├── products.json    # Данные товаров
│   │   ├── cart.json        # Данные корзин пользователей
│   │   └── sessions.json    # Активные сеансы
│   └── index.js             # Express сервер
├── src/
│   ├── components/
│   │   ├── Header.jsx       # Навигационное меню
│   │   ├── SearchBar.jsx    # Строка поиска с градиентной обводкой
│   │   ├── AuthPage.jsx     # Форма входа/регистрации
│   │   ├── ProductCard.jsx  # Карточка товара
│   │   ├── ProductModal.jsx # Модальное окно товара
│   │   ├── CartPage.jsx     # Страница корзины
│   │   └── AdminPanel.jsx   # Админ-панель
│   ├── App.jsx              # Главный компонент
│   ├── context.jsx          # Глобальное состояние (Context API)
│   ├── api.js               # API клиент (axios)
│   ├── main.jsx
│   └── index.css
├── package.json
├── vite.config.js
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/user` - Получить текущего пользователя

### Товары
- `GET /api/products` - Получить все товары
- `POST /api/products` - Добавить товар (только админ)
- `PUT /api/products/:id` - Обновить товар (только админ)
- `DELETE /api/products/:id` - Удалить товар (только админ)

### Корзина
- `GET /api/cart` - Получить корзину текущего пользователя
- `POST /api/cart` - Добавить товар в корзину
- `DELETE /api/cart/:productId` - Удалить товар из корзины

## Используемые технологии

- **Frontend**: React 19, Tailwind CSS, Axios
- **Backend**: Express.js, Node.js
- **Хранилище данных**: JSON файлы
- **Аутентификация**: Cookies, Sessions
- **UI**: Tailwind CSS с кастомными компонентами

## Примеры использования

### Регистрация нового пользователя
1. Нажмите "Вход / Регистрация" в хедере
2. Переключитесь на форму регистрации
3. Заполните данные и создайте аккаунт

### Добавление товара в корзину
1. Авторизуйтесь
2. Нажмите "Подробнее" на карточке товара
3. Выберите количество и нажмите "Добавить в корзину"

### Добавление товара (для админа)
1. Авторизуйтесь под админом
2. Нажмите "Админ панель" в хедере
3. Заполните форму и добавьте товар

## Примечания

- **Пароли**: В текущей реализации хранятся в открытом виде. Для production используйте хеширование (bcrypt)
- **Сеансы**: Хранятся в памяти (JSON файл). Для масштабирования используйте Redis
- **Изображения**: Используются placeholder-ссылки. Замените на реальные URL или upload функциональность
- **Поиск**: Работает в реальном времени по названию, описанию и категории

## Развертывание

Для production:
1. Используйте базу данных (MongoDB, PostgreSQL)
2. Добавьте аутентификацию JWT
3. Хешируйте пароли
4. Используйте HTTPS
5. Добавьте валидацию данных на обеих сторонах
6. Используйте переменные окружения для конфигурации

